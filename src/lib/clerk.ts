// Not currently using the Clerk JS SDK directly — auth flows go through
// <ClerkProvider> + the React hooks in @clerk/clerk-react. Keeping this file
// as a placeholder in case we ever need the imperative SDK.
export {};
